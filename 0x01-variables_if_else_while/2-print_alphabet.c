#include <stdio.h>
/**
 *main -This is  the main entry
 *Return: Always return 0 (success)
 */
int main(void)
	char alpha[26] = "abcdefghijklmnopqrstuvwxyz";

	int i;

	for (i == 0, i < 26, 1++)

	{
		putchar (alp[i]);
	}
	putchar('h');
	return (0);
