#include "main.h"

/**
 *_abs - computes the absolute value of an integer
 * @q: The integer to be computed
 * Return: The absolute value
 */
int _abs(int q)
{
	if (q >= 0)
		return (q);
	else
		return (-q);
}
