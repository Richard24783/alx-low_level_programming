#include "main.h"
#include <stdio.h>
/**
 * get_endianness - checks the endianess
 * Return: return 0 on big endian, 1 on little endian
 */
int get_endianness(void)
{
	unsigned int x = 1;
	char *c = (char *)&x;

	if (*c)

	return (1);

	else

	return (0);
}
